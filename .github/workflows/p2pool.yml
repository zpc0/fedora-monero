---
name: Build P2Pool package
permissions: {}
on:
  push:
    paths:
      - 'p2pool/p2pool.spec'

jobs:
  call-webhook:
    uses: zpc0/fedora-monero/.github/workflows/webhook.yml@master
    with:
      package-name: p2pool
    secrets:
      uuid: ${{ secrets.UUID }}

